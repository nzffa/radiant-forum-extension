%h1 Remove Post

%p 
  Are you sure you want to 
  %strong.warning permanently remove
  the post #{"by #{@post.reader.name}" if @post.reader} created at #{l @post.created_at} ?

%p.buttons
  = button_to("Delete Post", admin_post_url(@post), {:method => 'delete', :confirm => 'Really delete post?', :class => 'button'})
  or
  = link_to 'Cancel', admin_posts_url
